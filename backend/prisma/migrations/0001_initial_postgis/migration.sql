CREATE EXTENSION IF NOT EXISTS postgis;
-- Run `npx prisma migrate dev` to generate the remaining tables from schema.prisma
-- in development. The checked-in baseline enables the required database extension.
