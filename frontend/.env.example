# Backend REST API origin. Browser requests use the frontend's /api proxy so
# session cookies remain first-party; server-rendered pages call this URL directly.
API_BASE_URL=https://nyumba-pap-bew3p.deployments.nisoko.co.ke
